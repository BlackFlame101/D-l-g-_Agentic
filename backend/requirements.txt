# Delege Backend Dependencies
# Python 3.11+ required

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Agent Framework
agno>=1.5.0

# Task Queue
celery>=5.4.0
redis>=5.2.0

# Database
# 2.9.x pinned httpx<0.28 which conflicts with google-genai>=1.20. 2.15+ widened
# the range to httpx<0.29 and no longer pulls pyiceberg transitively.
supabase>=2.15.0,<3.0.0

# LLM (unified SDK, required by Agno's Gemini integration as well)
# Agno 2.x imports types (e.g. FileSearch) that only exist in google-genai >= 1.20
google-genai>=1.20.0

# Document Processing (for knowledge base)
pypdf>=5.0.0
python-docx>=1.1.0
python-multipart>=0.0.18

# Text Chunking & Embeddings
tiktoken>=0.8.0

# Environment Variables
python-dotenv>=1.0.1

# HTTP Client
# Note: google-genai >= 1.20 requires httpx >= 0.28, so we lift the old <0.28 cap.
httpx>=0.28.0,<0.29.0

# Utilities
pydantic>=2.10.0
pydantic-settings>=2.6.0
cryptography>=43.0.0

# -------- test (install with `pip install -r requirements.txt`) --------
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-timeout>=2.3.0
respx>=0.22.0
websockets>=13.0
